# HRM-Text 한국어 터미널 모델 진행 로그

작성일: 2026-05-23

## 현재 목표

`HRM-Text` 구조를 기반으로 한국어, 영어, 코드, 터미널, JSON/tool-call을 잘 처리하는 새 모델을 학습한다. 학습 방식은 HRM-Text 논문과 유사하게 instruction-response pair, PrefixLM attention, response-only loss를 따른다.

## 환경

| 항목 | 상태 |
|---|---|
| GPU | NVIDIA H200 8장 |
| VRAM | 장당 약 143GB free |
| Docker | 사용 불가 |
| `/dev/shm` | 2GB, HRM README 기본 `/dev/shm/sampled` 사용 불가 |
| 주 작업 경로 | `/home/work/.data` |
| HF token | `.env`의 `HF_TOKEN` 사용, 값은 로그에 기록하지 않음 |

## 문서 업데이트

- `HRM-Text/TRAINING_PLAN_2026-05-23.md` 생성.
- `HRM-Text/AVAILABLE_DATA.md` 수정.
- 잘못된 특수토큰 표기 수정:
  - `<|assistant|<` -> `<|assistant|>`
  - `<think|>`, `</think|>` -> `<think>`, `</think>`
- tokenizer 검증 코드의 `enc.text` 사용 오류 수정.
- Docker 불가, `/dev/shm` 2GB, `.data` 사용 방침 반영.
- tokenizer 비중과 model pre-training 비중을 분리해서 기록.

## 추가 데이터 확보

추가 데이터는 전체 다운로드보다 용량 제한 샘플링을 우선했다.

| 데이터 | 결과 | 위치 |
|---|---:|---|
| SWE-ZERO terminal/code trajectory sample | 1.0G, 53,868 rows | `/home/work/.data/huggingface/hrm_text_extra/sft/swe_zero_terminal_sft_sample.jsonl` |
| GLM-5.1 reasoning sample | 1.0G, 57,916 rows | `/home/work/.data/huggingface/hrm_text_extra/sft/glm_5_1_reasoning_sft_sample.jsonl` |
| structured-wikipedia tokenizer sample | 256M, 18,070 rows | `/home/work/.data/huggingface/hrm_text_extra/tokenizer_corpus/structured_wikipedia_en_sample.jsonl` |
| Claude Opus reasoning 8.7k | small raw snapshot 확보 | `/home/work/.data/huggingface/hrm_text_extra/raw/angrygiraffe__claude-opus-4.6-4.7-reasoning-8.7k` |
| DeepSeek-v4-Pro-Agent | 일부 파일만 확보, HF rate limit 발생 | `/home/work/.data/huggingface/hrm_text_extra/raw/TeichAI__DeepSeek-v4-Pro-Agent` |

DeepSeek agent trace는 파일 수가 4,000개 이상이라 비인증 요청으로는 rate limit이 심하다. 전체 다운로드는 HF token 기반 재시도 또는 streaming 샘플링 방식으로 처리한다.

## 추가 스크립트

| 파일 | 역할 |
|---|---|
| `HRM-Text/scripts/download_extra_training_data.py` | 큰 HF 데이터셋을 100MB~1GB 단위로 샘플링/다운로드 |
| `HRM-Text/scripts/train_koterm_tokenizer.py` | 한국어/터미널/tool-call용 byte-level BPE tokenizer 학습 |
| `HRM-Text/scripts/check_tokenizer_efficiency.py` | tokenizer chars/token 및 특수토큰 검증 |
| `HRM-Text/scripts/upload_folder_to_hf.py` | `.env`의 HF_TOKEN을 읽어 artifact 업로드 |
| `HRM-Text/scripts/watch_and_upload_hrm_checkpoints.py` | epoch checkpoint를 별도 프로세스로 HF 업로드 |
| `HRM-Text/scripts/prepare_sft_data.py` | 긴 샘플 overflow 처리, `<think>` 제거 옵션, 전처리 통계 저장 기능 추가 |

## 토크나이저 v1

위치:

`/home/work/.data/huggingface/trained_tokenizers/hrm-ko-terminal-131k-v1`

설정:

| 항목 | 값 |
|---|---:|
| 방식 | byte-level BPE |
| vocab size | 131,072 |
| normalization | NFC |
| 총 corpus cap | 2.5GiB |
| source별 cap | 256MiB |

검증 결과:

| sample | chars/token |
|---|---:|
| Korean general | 2.60 |
| Korean legal | 2.36 |
| Korean terminal instruction | 2.18 |
| Shell command | 2.68 |
| Tool-call JSON | 3.32 |
| Python code | 3.37 |
| English | 4.40 |

특수토큰 1-token 검증 통과:

- `<|im_start|>`
- `<|assistant|>`
- `<|tool_call|>`
- `<|terminal|>`
- `<|box_end|>`

판단:

1차 tokenizer로 충분히 사용 가능하다. 한국어와 터미널/JSON/code 모두 압축률이 과도하게 나쁘지 않고, HRM-Text condition/control token도 유지했다.

업로드:

`https://huggingface.co/LLM-OS-Models/HRM-Text-Ko-Terminal-Tokenizer-131K`

## 전처리 진행

SWE-ZERO 샘플 길이 점검:

- 첫 2,000개 샘플 기준 평균 약 3,686 tokens, p95 약 7,081 tokens, max 약 11,011 tokens.
- HRM paper 기본과 맞춘 `context-size=4097`에서는 긴 trajectory가 그대로는 실패한다.
- 해결 방식: response는 보존하고 instruction은 앞부분 시스템 지시 일부와 최근 맥락을 남기는 `truncate-instruction-middle` 사용.
- 현재 목적이 terminal/code action 학습이므로, 긴 샘플을 대량 drop하는 것보다 최근 터미널 맥락을 보존하는 절단이 더 낫다.

SWE-ZERO 전처리 완료:

```bash
python HRM-Text/scripts/prepare_sft_data.py \
  --train /home/work/.data/huggingface/hrm_text_extra/sft/swe_zero_terminal_sft_sample.jsonl \
  --tokenizer /home/work/.data/huggingface/trained_tokenizers/hrm-ko-terminal-131k-v1/tokenizer.json \
  --output /home/work/.data/hrm_text_prepared/sft_swe_zero_v1 \
  --epochs 5 \
  --context-size 4097 \
  --overflow-policy truncate-instruction-middle \
  --truncate-head-tokens 768 \
  --progress-interval 5000
```

결과:

- 입력 rows: 53,868
- 목적: SWE-ZERO 터미널/코딩 trajectory를 HRM-Text V1Dataset binary 포맷으로 변환
- 출력: `/home/work/.data/hrm_text_prepared/sft_swe_zero_v1`
- kept: 53,868
- truncated: 32,412
- dropped: 0
- tokens: 182,717,999
- avg sample: 3,392.0 tokens
- max sample: 4,096 tokens
- output size: 720MB

GLM reasoning 전처리 완료:

```bash
python HRM-Text/scripts/prepare_sft_data.py \
  --train /home/work/.data/huggingface/hrm_text_extra/sft/glm_5_1_reasoning_sft_sample.jsonl \
  --tokenizer /home/work/.data/huggingface/trained_tokenizers/hrm-ko-terminal-131k-v1/tokenizer.json \
  --output /home/work/.data/hrm_text_prepared/sft_glm_reasoning_v1 \
  --epochs 5 \
  --context-size 4097 \
  --overflow-policy truncate-instruction-middle \
  --truncate-head-tokens 768 \
  --strip-think-blocks \
  --condition-override direct
```

GLM reasoning은 논문 방식에 맞춰 teacher private reasoning인 `<think>...</think>` 블록을 제거하고, 남은 final answer 중심으로 response-only pretraining에 사용한다.

결과:

- 입력 rows: 57,916
- kept: 56,021
- truncated: 77
- dropped: 1,895
- tokens: 68,452,781
- avg sample: 1,221.9 tokens
- max sample: 4,096 tokens
- output size: 282MB

Merged dataset 생성 완료:

```bash
python HRM-Text/scripts/merge_prepared_sft_data.py \
  --inputs /home/work/.data/hrm_text_prepared/sft_swe_zero_v1 \
           /home/work/.data/hrm_text_prepared/sft_glm_reasoning_v1 \
  --output /home/work/.data/hrm_text_prepared/sft_swe_glm_mix_v1 \
  --epochs 5 \
  --copy-tokenizer
```

결과:

- 출력: `/home/work/.data/hrm_text_prepared/sft_swe_glm_mix_v1`
- samples: 109,889
- tokens: 251,170,780
- avg sample: 2,285.7 tokens
- max sample: 4,096 tokens
- dataset loader smoke test 통과: rank 0 기준 local batch 32,768 tokens 생성 확인.

## 학습 의존성

- `coolname` 누락으로 `pretrain.py` import가 실패해서 `pip install coolname` 완료.
- `flash_attn_interface` 누락 확인.
- 공식 Dao-AILab FlashAttention repo 기준 FA3는 `hopper/` 서브패키지이고, H100/H800 계열 Hopper + CUDA 12.3 이상을 요구하며 CUDA 12.8을 권장한다.
- 현재 환경은 H200, CUDA toolkit 12.9, PyTorch CUDA 12.8이라 조건에 맞는다.
- `/home/work/.data/src/flash-attention`에 official repo shallow clone 완료.
- `hopper/setup.py install --user`로 FA3 빌드 완료.
- `flash_attn_interface` import 통과.
- `pretrain.py` import 통과.
- FA3 source/build/cache 사용량: 약 1GB.

## Pilot 학습

config resolve 통과 후 pilot 학습 완료:

```bash
python pretrain.py --cfg job \
  arch/size@arch=B \
  data.path=/home/work/.data/hrm_text_prepared/sft_swe_glm_mix_v1 \
  +checkpoint_path=/home/work/.data/hrm_text_checkpoints/koterm_b_swe_glm_pilot_v1 \
  +project_name=HRM-Ko-Terminal \
  +run_name=koterm_b_swe_glm_pilot_v1 \
  epochs=1 \
  global_batch_size=262144 \
  lr_warmup_steps=100 \
  +log_interval=5 \
  checkpoint_interval=1
```

예상:

- total tokens: 251,170,780
- global batch: 262,144 tokens
- expected steps: 약 958
- GPU: H200 8장
- 목적: 전체 장기 학습 전 데이터셋, FA3, FSDP2, tokenizer, checkpoint 저장 경로 검증.

결과:

- 상태: 성공
- 실제 progress bar: 965 iterations
- wall time: 약 7분 38초
- peak VRAM: B pilot 기준 장당 약 38GB
- num params: 435,159,040
- final train/loss: 3.00653
- final train/accuracy: 0.46379
- final train/exact_accuracy: 0
- W&B: offline run으로 저장, cloud sync는 하지 않음
- checkpoint size: 6.6GB
- checkpoint path: `/home/work/.data/hrm_text_checkpoints/koterm_b_swe_glm_pilot_v1`

판단:

- tokenizer, prepared dataset, PrefixLM/response-only loader, FA3, FSDP2 checkpoint 저장까지 end-to-end로 정상 동작한다.
- B pilot은 H200 VRAM을 장당 약 38GB만 사용했으므로 너무 보수적이다.
- 다음 실제 학습은 L 또는 XL로 올리고, global batch도 262K 이상부터 probe하는 것이 맞다.
- 단, 장기 학습 전에는 한국어 일반/법률 instruction 데이터와 tool-call/terminal 데이터 비중을 더 보강해야 한다. 현재 pilot mix는 SWE terminal/code 비중이 token 기준 약 72.7%라 terminal 검증에는 좋지만 최종 Korean general model mix로는 과하다.

업로드:

- repo: `https://huggingface.co/LLM-OS-Models/HRM-Text-Ko-Terminal-B-SWE-GLM-Pilot`
- 업로드 형식: raw HRM-Text FSDP2 checkpoint
- 업로드 검증: HF repo file listing 22개 확인.
- 주의: 바로 Transformers에서 로드하는 모델이 아니다. 배포용은 `conversion/convert_to_hf.py`로 별도 변환해야 한다.

## 업로드 정책

- tokenizer처럼 작은 artifact는 검증 통과 시점에 업로드.
- 학습 checkpoint는 step 단위가 아니라 epoch 단위 업로드.
- 업로드는 학습 프로세스 안에서 하지 않고 watcher 프로세스로 분리.
- 최종 배포용은 raw FSDP2 checkpoint가 아니라 `conversion/convert_to_hf.py`로 변환한 model-only artifact를 업로드.

## 다음 작업

1. HRM cleaned 328G를 새 tokenizer로 재토큰화해서 사전학습 mix에 포함.
2. 한국어 위키/법령/조례/행정규칙/판례를 instruction-response task로 변환.
3. ToolBench와 local terminal dataset을 tool/terminal task로 변환.
4. SWE/GLM prepared dataset까지 포함한 balanced pretraining dataset 생성.
5. H200 8장 기준 L/XL batch size probe.
6. 안정 batch 확인 후 장기 학습 및 epoch 단위 HF 업로드 watcher 실행.

## 2026-05-23 데이터 mix 재정리

- 새 기준 문서: `HRM-Text/PRETRAINING_SFT_DATA_MIX_2026-05-23.md`
- 결정: SFT 후보 데이터도 사전학습에서 제외하지 않고 모두 instruction-response PrefixLM pretraining mix에 포함한다.
- SFT는 사전학습 이후 같은 계열의 고품질 subset을 더 엄격한 포맷으로 한 번 더 학습한다.
- HRM 기존 cleaned dataset 328G는 사용한다. 단, 새 tokenizer를 쓰므로 기존 token id 바이너리를 그대로 섞지 않고 JSONL/parquet 원문에서 다시 토큰화한다.
- 현재 B pilot은 SWE+GLM 251.2M tokens만 사용했기 때문에 최종 mix가 아니다.

## 2026-05-23 전처리 스크립트 확장

- `scripts/prepare_sft_data.py`를 JSONL 단일 파일 전용에서 JSONL/parquet 다중 입력, 디렉터리 재귀 입력, row/token cap 입력으로 확장했다.
- HRM cleaned parquet의 `synth,direct`, `noisy,cot` 같은 복합 condition을 `synth`, `cot`, `noisy`, `direct` bucket으로 정규화하도록 했다.
- smoke test:
  - 입력: HRM cleaned `data_clustered/openmathinstruct2/direct.parquet`
  - 출력: `/home/work/.data/hrm_text_prepared/smoke_hrm_parquet_v1`
  - 결과: 292 samples, 20,028 tokens, max sample 802 tokens
  - 판단: HRM 328G 원문 parquet를 새 tokenizer로 V1Dataset 재패킹하는 경로가 동작한다.

## 2026-05-23 KoHRM-Text-1.4B 학습 시작

- 모델 이름 기준: `KoHRM-Text-1.4B`
- arch: `XL`
- 새 tokenizer vocab: 131,072
- 추정 파라미터: 약 1.384B
- dataset: `/home/work/.data/hrm_text_prepared/koterm_pretrain_mix_v1`
- dataset tokens: 711,277,327
- context: 4096 tokens (`metadata.max_seq_len=4097`, AR shift 포함)
- dtype: bfloat16
- GPUs: 8 x H200

시도한 batch:

| global batch | per GPU tokens | 결과 |
|---:|---:|---|
| 524,288 | 65,536 | 실패. L size에서도 final logits buffer가 `65,536 x 131,072` bf16 약 16GB를 추가 요구해 OOM |
| 262,144 | 32,768 | L size에서 180 step까지 정상. 논문 설정으로 맞추기 위해 중단 |
| 262,144 | 32,768 | XL / KoHRM-Text-1.4B 360/2713 step에서 OOM. bp warmup으로 backward depth가 증가한 뒤 final logits buffer `32,768 x 131,072` bf16 8GB 추가 할당에 실패 |
| 196,608 | 24,576 | XL / KoHRM-Text-1.4B 재시작. 논문 global batch와 동일한 안정 설정 |

비교:

- HRM-Text 논문은 bfloat16, context 4096, vocab 65,536, global batch 196,608 tokens를 사용했다.
- 논문 1B run은 16 x H100 기준이라 GPU당 local batch는 12,288 tokens다.
- 262,144 run은 8 x H200에서 GPU당 local batch 32,768 tokens라 논문 GPU당 local batch의 약 2.67배였지만, 131,072 vocab의 logits 메모리와 HRM bp warmup 증가가 겹쳐 실패했다.
- 196,608 run은 논문 global batch와 같고, 8 x H200에서 GPU당 local batch 24,576 tokens다. 여전히 논문 16 x H100의 GPU당 12,288 tokens 대비 2배다.

전체 학습 규모 기준:

- HRM-Text 논문/기존 cleaned 데이터 축은 약 40B unique tokens로 본다.
- 우리가 추가하려는 SWE-ZERO, GLM reasoning, 한국 법령/조례/행정규칙/판례, ToolBench, local terminal/code 데이터를 모두 task화해서 포함하면 추가 약 10~12B tokens로 추정한다.
- 따라서 KoHRM-Text full pretraining 목표 규모는 보수적으로 45~48B, 기본 계획으로 50~52B, 공격적으로 structured wiki와 더 많은 SWE/GLM을 포함하면 55B+까지 가능하다.
- 현재 `koterm_pretrain_mix_v1` 711.3M tokens run은 full training이 아니라 XL/batch/tokenizer/data pipeline 안정성 검증용이다.

## 2026-05-23 staged 학습 전환

사용자 요청대로 전처리 완료까지 GPU를 기다리게 두지 않고, 완료된 데이터부터 학습한 뒤 새 데이터가 생기면 checkpoint에서 이어 학습하는 방식으로 전환했다.

현재 실행:

| 작업 | 상태 | 로그/경로 |
|---|---|---|
| HRM cleaned fast-cap retokenization | 실행 중 | `/home/work/.data/hrm_text_logs/koterm_hrm_cleaned_fastcap_v1.log` |
| KoHRM-Text-1.4B stage-0 training | 실행 중 | `/home/work/.data/hrm_text_logs/KoHRM-Text-1.4B-stage0-available-mix-gbs172.log` |
| stage-0 checkpoint | 생성 대기 | `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0-available-mix-gbs172` |
| HF upload watcher | 실행 중 | `/home/work/.data/hrm_text_logs/KoHRM-Text-1.4B-stage0-hf-upload-watcher.log` |

stage-0 설정:

- model: `KoHRM-Text-1.4B`
- arch: `XL`
- params: 1,384,120,320
- GPUs: 8 x H200
- dataset: `/home/work/.data/hrm_text_prepared/koterm_pretrain_mix_v1`
- tokens: 711,277,327
- global batch: 172,032 tokens
- per GPU tokens: 21,504
- total steps: 4,134
- context: 4096 tokens
- dtype: bfloat16
- checkpoint interval: 1 epoch
- HF repo target: `LLM-OS-Models/KoHRM-Text-1.4B`

이유:

- 262,144 global batch는 XL에서 OOM이 확인됐다.
- 196,608 global batch는 논문과 동일하지만, 우리 vocab은 131,072라 logits memory가 논문 대비 약 2배다.
- 장기 run에서는 중간 VRAM 상승, bp warmup, checkpoint/save 시점 여유까지 고려해야 하므로 `172032`을 기본값으로 잡았다.

추가 문서:

- `STAGED_TRAINING_RUNBOOK_2026-05-23.md`
- `MODEL_CARD_KoHRM-Text-1.4B.md`

## 2026-05-23 resume schedule 보정

upstream HRM-Text `pretrain.py`는 checkpoint에서 model/optimizer/EMA/carry를 불러오지만, global step과 LR schedule은 새 run마다 0부터 시작한다. stage-wise pretraining에서는 기술적으로 학습을 이어갈 수 있지만, 논문식 long pretraining schedule과 완전히 같지는 않다.

보정:

- `resume_step_offset` 추가.
- `total_steps_override` 추가.
- tqdm progress도 offset에서 시작하도록 수정.

사용 방식:

- stage-0 완료 후 stage-1 시작 시 `resume_step_offset=4134`를 지정한다.
- full plan이 50B tokens이고 batch 172,032이면 전체 optimizer step은 약 290,643 step이다.
- stage-wise로 데이터를 추가하더라도 LR warmup/cosine schedule은 이 전체 step 기준으로 이어간다.

## 2026-05-23 data_io tokenizer fast-cap 패치 기록

로컬 `/home/work/.projects/LLM-OS-Models/Terminal/data_io` clone에 Rust tokenizer 변경을 적용했다.

목적:

- HRM cleaned 328G 전체를 새 tokenizer로 다시 처리하되, GPU가 대기하지 않도록 fast-cap 산출물을 먼저 만든다.
- HF snapshot symlink 때문에 파일 스캔이 누락되지 않게 한다.
- 대형 source를 prefix별 cap으로 제한해 stage-1 입력을 빠르게 확보한다.

주요 변경:

- `WalkDir::follow_links(true)`
- stream reader callback 조기 종료 지원
- `--epochs-for-caps`
- `--cap prefix=max_rows`
- 복합 condition 분리 및 `direct` fallback
- metadata에 `row_limit` 기록

현재 fast-cap 실행 산출물:

- output: `/home/work/.data/hrm_text_tokenized/koterm_hrm_cleaned_fastcap_v1`
- log: `/home/work/.data/hrm_text_logs/koterm_hrm_cleaned_fastcap_v1.log`

## 2026-05-23 stage-0 완료 및 HRM stage-1 준비

stage-0 available-mix run은 완료됐다.

- checkpoint: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0-available-mix-gbs172/fsdp2_epoch_1`
- carry files: 8/8
- final train/loss: 2.05764
- final train/accuracy: 0.62219
- final train/exact_accuracy: 0.25763
- params: 1,384,120,320

HF 업로드:

- repo: `LLM-OS-Models/KoHRM-Text-1.4B`
- uploaded artifact: raw HRM-Text FSDP2 epoch-1 checkpoint plus carry/config/tokenizer files
- uploaded size: 약 25.0GB
- model card commit: `db5a5b3e282763bcdbcecc6982772b7405384cf9`

현재 GPU 우선 정책에 따라 stage-0 checkpoint에서 바로 이어서 stage0b를 수동 실행 중이다.

- run: `KoHRM-Text-1.4B-stage0b-debug-launch2`
- resume_from: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0-available-mix-gbs172`
- dataset: `/home/work/.data/hrm_text_prepared/koterm_pretrain_mix_v1`
- global batch: 196,608 tokens
- resume_step_offset: 4,134
- total_steps_override: 290,643
- 목적: HRM full dataset V1Dataset 생성 중에도 8 x H200 GPU를 idle로 두지 않고 optimizer/EMA/carry를 이어 학습한다.

HRM cleaned fast-cap tokenized snapshot은 V1Dataset으로 샘플링 완료했다.

- input tokenized root: `/home/work/.data/hrm_text_tokenized/koterm_hrm_cleaned_fastcap_stage1_snapshot`
- output dataset: `/home/work/.data/hrm_text_prepared/koterm_hrm_cleaned_fastcap_stage1_v1`
- metadata total_length: 14,554,291,763 tokens
- max_seq_len: 4,097
- backing `tokens.npy`: 155,673,876,292 bytes
- disk usage: 약 148GB
- report unique sampled: 14,215,204,055 tokens out of 37,929,994,320 candidate tokens

주의:

- `metadata.total_length`가 다음 학습 epoch 길이 기준이다.
- `tokens.npy`는 전체 source token backing mmap이라 `metadata.total_length`보다 크다.
- 다음 학습은 stage0b checkpoint 저장 직후 이 HRM stage-1 dataset으로 이어간다.

## 2026-05-23 stage0b 완료 및 stage-1 HRM fast-cap 학습 시작

stage0b는 `koterm_pretrain_mix_v1` 711.3M token을 stage-0 checkpoint에서 이어서 한 번 더 학습했다.

- run: `KoHRM-Text-1.4B-stage0b-debug-launch2`
- resume_from: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0-available-mix-gbs172`
- checkpoint: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0b-debug-launch2/fsdp2_epoch_1`
- carry files: 8/8
- global batch: 196,608 tokens
- resume_step_offset: 4,134
- actual end step: 7,765
- final train/loss: 1.90957
- final train/accuracy: 0.62765
- final train/exact_accuracy: 0.27138

종료 시 rank0에서 SIGSEGV가 발생했지만, wandb summary 출력과 checkpoint/carry 저장은 완료된 상태였다. 체크포인트 파일을 확인한 뒤 이 checkpoint를 다음 stage의 `resume_from`으로 사용했다.

stage0b raw FSDP2 checkpoint도 HF에 업로드했다.

- repo: `LLM-OS-Models/KoHRM-Text-1.4B`
- upload source: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0b-debug-launch2`
- uploaded artifact: `fsdp2_epoch_1`, carry/config/tokenizer/model card
- staged size: 약 23.33 GiB
- upload result: 완료

현재 stage-1은 HRM cleaned fast-cap V1Dataset으로 실행 중이다.

- run: `KoHRM-Text-1.4B-stage1-hrm-fastcap-gbs262`
- resume_from: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage0b-debug-launch2`
- dataset: `/home/work/.data/hrm_text_prepared/koterm_hrm_cleaned_fastcap_stage1_v1`
- dataset total_length: 14,554,291,763 tokens
- checkpoint_path: `/home/work/.data/hrm_text_checkpoints/KoHRM-Text-1.4B-stage1-hrm-fastcap-gbs262`
- global batch: 262,144 tokens
- local token slots per GPU: 32,768
- resume_step_offset: 7,765
- total_steps_override: 63,300
- observed VRAM: GPU0 약 118GB, 나머지 약 116GB
- observed utilization: 8 x H200 모두 99%
- observed speed after compile: 약 1.09-1.10 sec/step, 약 238k-240k tokens/sec
- rough ETA from stable speed: 약 16.7-17.0 hours

배치 결정:

- `196,608`은 stage0b에서 안정적으로 1.31 step/sec를 기록했지만 VRAM 사용량이 약 65-67GB로 낮았다.
- `262,144`는 stage-1 초반에는 VRAM 116-118GB로 동작했지만, 후속 compile graph에서 `32768 x 131072` bf16 logits buffer 8GB 추가 할당이 필요해 OOM이 발생했다.
- OOM 시점은 약 8,435 step 부근이며, 저장 checkpoint는 생성되지 않았다.
- stage-1은 즉시 `229,376` batch로 재시작했다. 이 설정은 local token slots/GPU가 28,672이고, VRAM은 GPU0 약 105GB, 나머지 약 103GB로 안정적이다.
- 현재 정책은 GPU 학습 우선이므로 `229,376`을 유지한다. 추가 OOM이 발생하면 `196,608`로 되돌려 재개한다.

HF 업로드 정리:

- 메인 repo `LLM-OS-Models/KoHRM-Text-1.4B`는 `model.safetensors`, `config.json`, tokenizer, README만 남기도록 정리했다.
- HF unsafe scan 경고를 만들던 raw DCP 파일(`fsdp2_epoch_1/.metadata`, `__*.distcp`)과 carry pt, backing tokenizer npy 파일은 메인 repo에서 삭제했다.
- 삭제 commit: `https://huggingface.co/LLM-OS-Models/KoHRM-Text-1.4B/commit/cdaeda8d20157b304d480b7e3c8dde6e47db9425`
- stage0b safetensors export: `/home/work/.data/hrm_text_hf_exports/KoHRM-Text-1.4B-stage0b-safetensors`
- main HF repo now contains only: `.gitattributes`, `README.md`, `config.json`, `model.safetensors`, `tokenizer.json`, `tokenizer_config.json`
- raw FSDP2 checkpoint upload target은 별도 repo `LLM-OS-Models/KoHRM-Text-1.4B-raw-checkpoints`로 분리한다.

## 2026-05-23 법률 원문 full 전처리 및 HRM 328G full 재토큰화

한국어 법률 데이터는 기존 83.1M token task 샘플만으로는 부족하므로, 법령/자치법규 Markdown 원문 전체를 별도 raw response target V1Dataset으로 만들었다.

- script: `scripts/build_korean_legal_raw_corpus.py`
- raw JSONL: `/home/work/.data/huggingface/hrm_text_extra/sft/korean_legal_raw_full_20260523.jsonl`
- raw JSONL size: 약 1.8G
- roots:
  - `law=/home/work/.projects/LLM-OS-Models/Terminal/HRM-Text/legalize-kr`
  - `ordinance=/home/work/.projects/LLM-OS-Models/Terminal/HRM-Text/ordinance-kr`
- JSONL rows: 227,791
- by source:
  - `korean_legal_raw_law`: 25,899 chunks
  - `korean_legal_raw_ordinance`: 201,892 chunks

V1Dataset 전처리 결과:

- output: `/home/work/.data/hrm_text_prepared/korean_legal_raw_full_v1`
- kept rows: 227,788
- dropped response-too-long rows: 3
- truncated rows: 0
- tokens: 308,893,496
- max sample length: 3,574
- disk usage: 약 1.2G

기존 HRM 328G cleaned data도 최종 40B급 학습에 반드시 포함해야 하므로 cap 없는 full 재토큰화를 시작했다.

- input snapshot: `/home/work/.data/huggingface/hub/datasets--sapientinc--HRM-Text-data-io-cleaned-20260515/snapshots/f033bc0e1a81634385093afc60445a52a7ade64a`
- raw cache size: 약 328G
- output tokenized root: `/home/work/.data/hrm_text_tokenized/koterm_hrm_cleaned_full_nocap_v1`
- tokenizer: `/home/work/.data/huggingface/trained_tokenizers/hrm-ko-terminal-131k-v1/tokenizer.json`
- caps: all major prefixes set to `0`, meaning no cap
- first launch: 79 threads caused pretraining step ETA to regress, so it was stopped.
- current launch: `taskset -c 96-111`, `nice -n 10`, `ionice -c3`, 15 tokenizer threads.

정책:

- GPU 학습 우선이다. full 재토큰화는 필요하지만, 학습 step/sec를 크게 떨어뜨리면 thread count를 줄인다.
- 현재 stage-1 14.55B tokens는 최종 학습량이 아니다. 논문 기준 40B unique/effective tokens에 맞추려면 stage-1 이후 HRM full nocap 산출물, 법률 원문 full, terminal/tool-call/code 데이터를 추가 stage로 이어 학습한다.
